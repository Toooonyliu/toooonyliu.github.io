import {chromium} from '@playwright/test';const b=await chromium.launch({headless:true,args:['--enable-webgl','--use-angle=swiftshader','--enable-unsafe-swiftshader']});const p=await b.newPage({viewport:{width:1400,height:1200}});await p.goto('http://127.0.0.1:4174/');await p.getByRole('navigation').getByRole('button',{name:'Passport',exact:true}).click();await p.getByRole('button',{name:'Expand panel',exact:true}).click();await p.getByTestId('flow-current').locator('.past-flight-row').first().click();await p.waitForTimeout(1000);console.log(JSON.stringify(await p.evaluate(()=>({active:document.activeElement?.outerHTML.slice(0,250),nodes:['.device-screen','.flow-stack','.flow-body','[data-testid=flow-current]','.map-panel-layout','.travel-panel','.keyboard-dock'].map(s=>[s,...[...document.querySelectorAll(s)].map(e=>({rect:e.getBoundingClientRect().toJSON(),scroll:e.scrollTop,style:e.getAttribute('style'),cls:e.className,data:{...e.dataset}}))])}))));await b.close();
